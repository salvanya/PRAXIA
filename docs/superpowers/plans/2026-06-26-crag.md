# Subgrafo CRAG — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el `rag_node` plano por un subgrafo CRAG correctivo (retrieve → rerank → juez de relevancia → reformular/reintentar → síntesis buffered con citas → juez de groundedness → emitir o abstener).

**Architecture:** Un subgrafo LangGraph **puro** (`graph/rag_subgraph.py`) computa y devuelve `{answer, sources, grounded, abstained, reranked}`; el `rag_node` externo invoca el subgrafo y hace **todo** el streaming SSE al final (buffer-then-stream: nada llega al usuario sin verificación). La lógica de cada paso vive en módulos puros bajo `rag/` (`rerank.py`, `judges.py`, `reformulate.py`); los nodos del subgrafo son finos y los llaman.

**Tech Stack:** Python 3.12 · LangGraph 0.2 · langchain-ollama (Gemma `e4b` jueces/reformulador, `12b` síntesis) · sentence-transformers `CrossEncoder` (`bge-reranker-v2-m3`) · pytest (asyncio auto).

## Global Constraints

- **Inferencia 100% local por Ollama** (`http://localhost:11434`); cero red saliente desde el código de Praxia salvo Ollama/Qdrant/Postgres locales (CLAUDE.md §0).
- **Salida estructurada obligatoria** en jueces/reformulador: `ChatOllama.with_structured_output(PydanticModel)`. Prohibido parsear con regex (CLAUDE.md §4).
- **Aislamiento multi-tenant**: todo retrieve filtra por `practice_id` (CLAUDE.md §0.5).
- **No tocar las escrituras**: `sql_stub`/`action_stub` quedan intactos; las escrituras siguen sin existir (DoD §6.3).
- **Sin deps nuevas**: `CrossEncoder` viene en `sentence-transformers==3.*` (ya en `requirements.txt`).
- **Gates** (CLAUDE.md §6): `ruff check . && ruff format .`, `mypy app/`, `pytest -q` verdes. ruff line-length=100, reglas E/F/I/UP/B. mypy `disallow_untyped_defs=true` (toda función tipada).
- **Commits LIMPIOS**: sin ninguna atribución a Claude/Anthropic (CLAUDE.md §6). Mensajes single-line en `git commit -m "..."` (PowerShell 5.1 mangea comillas dobles embebidas; sin comillas dobles dentro del mensaje).
- **Comandos** (desde la raíz del repo, PowerShell — no `cd x && y`): tests con `backend\.venv\Scripts\python -m pytest backend/tests ...`.

---

### Task 1: `app/llm.py` — factory `make_llm` + DRY de ChatOllama

**Files:**
- Create: `backend/app/llm.py`
- Create: `backend/tests/test_llm.py`
- Modify: `backend/app/graph/router.py` (`_router_llm`)
- Modify: `backend/app/graph/nodes.py` (`_chitchat_llm`)
- Modify: `backend/app/rag/synthesize.py` (`_default_llm`)

**Interfaces:**
- Produces: `make_llm(model: str, temperature: float = 0.0) -> Any` — construye un `ChatOllama(model=..., base_url=settings.ollama_base_url, temperature=...)`.

- [ ] **Step 1: Write the failing test** — `backend/tests/test_llm.py`

```python
from app import llm


def test_make_llm_builds_chatollama_with_params():
    obj = llm.make_llm("gemma4:e4b", temperature=0.0)
    assert obj.model == "gemma4:e4b"
    assert obj.temperature == 0.0


def test_make_llm_uses_settings_base_url():
    from app.config import get_settings

    obj = llm.make_llm("gemma4:12b", temperature=0.3)
    assert obj.base_url == get_settings().ollama_base_url
```

- [ ] **Step 2: Run test to verify it fails**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_llm.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.llm'`.

- [ ] **Step 3: Create `backend/app/llm.py`**

```python
from typing import Any

from app.config import get_settings


def make_llm(model: str, temperature: float = 0.0) -> Any:
    """Factory central de ChatOllama (inferencia local). Apunta siempre al
    base_url de settings; centraliza la construcción que antes estaba repetida
    en router/chitchat/síntesis."""
    from langchain_ollama import ChatOllama

    return ChatOllama(
        model=model,
        base_url=get_settings().ollama_base_url,
        temperature=temperature,
    )
```

- [ ] **Step 4: Refactor the three call sites to use `make_llm` (sin cambio de comportamiento)**

`backend/app/graph/router.py` — reemplazar `_router_llm`:

```python
def _router_llm() -> Any:
    from app.llm import make_llm

    return make_llm("gemma4:e4b", temperature=0.0)
```

**Importante:** en `router.py`, `_router_llm` era el único consumidor de `get_settings`; tras el refactor el import `from app.config import get_settings` queda sin uso → **eliminálo** del módulo (si no, ruff F401). Dejá `from typing import Any, Literal` (siguen en uso). En `nodes.py` y `synthesize.py`, en cambio, **mantené** `from app.config import get_settings` (sus `_chitchat_llm`/`_default_llm` siguen usando `get_settings().ollama_model`).

`backend/app/graph/nodes.py` — reemplazar `_chitchat_llm`:

```python
def _chitchat_llm() -> Any:
    from app.llm import make_llm

    return make_llm(get_settings().ollama_model, temperature=0.3)
```

`backend/app/rag/synthesize.py` — reemplazar `_default_llm`:

```python
def _default_llm() -> Any:
    from app.llm import make_llm

    return make_llm(get_settings().ollama_model, temperature=0.1)
```

- [ ] **Step 5: Run test + verificar que no rompimos nada**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_llm.py backend/tests/test_router.py backend/tests/test_nodes.py backend/tests/test_synthesize.py -q`
Expected: PASS (todos).

- [ ] **Step 6: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 7: Commit**

```bash
git add backend/app/llm.py backend/tests/test_llm.py backend/app/graph/router.py backend/app/graph/nodes.py backend/app/rag/synthesize.py
git commit -m "refactor(llm): factory make_llm y DRY de ChatOllama en router/chitchat/sintesis"
```

---

### Task 2: `rag/rerank.py` — reranker cross-encoder con floor + fallback

**Files:**
- Create: `backend/app/rag/rerank.py`
- Create: `backend/tests/test_rerank.py`
- Modify: `backend/app/config.py` (agregar `rerank_model`, `rerank_min_score`)

**Interfaces:**
- Consumes: `Chunk` (`app.models`), `get_settings().top_k` (=5), `rerank_model`, `rerank_min_score`.
- Produces: `async def rerank(query: str, chunks: list[Chunk]) -> list[Chunk]` — reordena por score del cross-encoder (sigmoid del logit), descarta `< rerank_min_score`, corta a `top_k`. Ante error del modelo: fallback a `chunks[:top_k]` (orden denso). `[]` si entra `[]`.

- [ ] **Step 1: Add config fields** — `backend/app/config.py`, después de `top_k: int = 5`:

```python
    top_k: int = 5
    rerank_model: str = "BAAI/bge-reranker-v2-m3"
    rerank_min_score: float = 0.2
```

- [ ] **Step 2: Write the failing test** — `backend/tests/test_rerank.py`

```python
from app.models import Chunk
from app.rag import rerank


def _c(text: str, doc_id: str) -> Chunk:
    return Chunk(text=text, page=None, chunk_index=0, document_id=doc_id, title="T", doc_type="x")


class FakeCE:
    def __init__(self, scores: list[float]):
        self._scores = scores

    def predict(self, pairs: list[tuple[str, str]]) -> list[float]:
        return self._scores


class BoomCE:
    def predict(self, pairs: list[tuple[str, str]]) -> list[float]:
        raise RuntimeError("model down")


async def test_rerank_orders_by_score_desc(monkeypatch):
    chunks = [_c("a", "1"), _c("b", "2"), _c("c", "3")]
    monkeypatch.setattr(rerank, "_model", lambda: FakeCE([0.0, 5.0, 2.0]))
    out = await rerank.rerank("q", chunks)
    assert [c["document_id"] for c in out] == ["2", "3", "1"]


async def test_rerank_drops_below_floor(monkeypatch):
    chunks = [_c("a", "1"), _c("b", "2")]
    # sigmoid(5)~0.99 (keep) ; sigmoid(-10)~0 (< 0.2 floor, drop)
    monkeypatch.setattr(rerank, "_model", lambda: FakeCE([5.0, -10.0]))
    out = await rerank.rerank("q", chunks)
    assert [c["document_id"] for c in out] == ["1"]


async def test_rerank_caps_to_top_k(monkeypatch):
    chunks = [_c(str(i), str(i)) for i in range(10)]
    monkeypatch.setattr(rerank, "_model", lambda: FakeCE([float(i) for i in range(10)]))
    out = await rerank.rerank("q", chunks)
    assert len(out) == 5
    assert [c["document_id"] for c in out] == ["9", "8", "7", "6", "5"]


async def test_rerank_falls_back_to_dense_order_on_error(monkeypatch):
    chunks = [_c("a", "1"), _c("b", "2")]
    monkeypatch.setattr(rerank, "_model", lambda: BoomCE())
    out = await rerank.rerank("q", chunks)
    assert [c["document_id"] for c in out] == ["1", "2"]


async def test_rerank_empty_returns_empty():
    assert await rerank.rerank("q", []) == []
```

- [ ] **Step 3: Run test to verify it fails**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_rerank.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.rag.rerank'`.

- [ ] **Step 4: Create `backend/app/rag/rerank.py`**

```python
import asyncio
import logging
import math
from functools import lru_cache
from typing import Any

from app.config import get_settings
from app.models import Chunk

logger = logging.getLogger(__name__)


@lru_cache
def _model() -> Any:
    from sentence_transformers import CrossEncoder

    return CrossEncoder(get_settings().rerank_model)


def _sigmoid(x: float) -> float:
    return 1.0 / (1.0 + math.exp(-x))


def _score(query: str, chunks: list[Chunk]) -> list[float]:
    pairs = [(query, c["text"]) for c in chunks]
    raw = _model().predict(pairs)
    return [_sigmoid(float(s)) for s in raw]


async def rerank(query: str, chunks: list[Chunk]) -> list[Chunk]:
    """Reordena los candidatos por relevancia (cross-encoder), descarta los que
    no superan el floor y corta a top_k. Ante fallo del modelo, degrada al orden
    denso de entrada en vez de romper el turno (CLAUDE.md: rerank siempre, pero
    resiliencia > pureza ante un fallo transitorio de carga)."""
    if not chunks:
        return []
    s = get_settings()
    try:
        scores = await asyncio.to_thread(_score, query, chunks)
    except Exception:
        logger.warning("reranker fallo; fallback al orden denso", exc_info=True)
        return chunks[: s.top_k]
    ranked = sorted(zip(chunks, scores, strict=True), key=lambda cs: cs[1], reverse=True)
    kept = [chunk for chunk, score in ranked if score >= s.rerank_min_score]
    return kept[: s.top_k]
```

- [ ] **Step 5: Run test to verify it passes**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_rerank.py -q`
Expected: PASS (5 tests).

- [ ] **Step 6: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 7: Commit**

```bash
git add backend/app/rag/rerank.py backend/tests/test_rerank.py backend/app/config.py
git commit -m "feat(rag): reranker bge-reranker-v2-m3 con floor, top_k y fallback al orden denso"
```

---

### Task 3: `rag/judges.py` — juez de relevancia + juez de groundedness

**Files:**
- Create: `backend/app/rag/judges.py`
- Create: `backend/tests/test_judges.py`
- Modify: `backend/app/rag/synthesize.py` (agregar helper público `chunks_text`, compartido con Task 4)

**Interfaces:**
- Consumes: `make_llm` (`app.llm`), `Chunk`, `chunks_text` (`app.rag.synthesize`).
- Produces (compartido): `def chunks_text(chunks: list[Chunk]) -> str` en `synthesize.py` — formatea chunks como lista para prompts de jueces/reformulador (sin marcas de cita). Lo reusa Task 4.
- Produces:
  - `class RelevanceVerdict(BaseModel): sufficient: bool; reason: str`
  - `class GroundednessVerdict(BaseModel): grounded: bool; reason: str`
  - `async def judge_relevance(query: str, chunks: list[Chunk], llm: Any = None) -> RelevanceVerdict`
  - `async def judge_groundedness(answer: str, chunks: list[Chunk], llm: Any = None) -> GroundednessVerdict`

- [ ] **Step 1: Write the failing test** — `backend/tests/test_judges.py`

```python
from app.models import Chunk
from app.rag import judges


def _c() -> Chunk:
    return Chunk(
        text="La consulta dura 60 min.", page=None, chunk_index=0,
        document_id="1", title="T", doc_type="x",
    )


class FakeStructured:
    def __init__(self, value):
        self._value = value

    async def ainvoke(self, messages):
        return self._value


class FakeLLM:
    def __init__(self, value):
        self._value = value

    def with_structured_output(self, schema):
        return FakeStructured(self._value)


async def test_judge_relevance_parses_sufficient():
    llm = FakeLLM(judges.RelevanceVerdict(sufficient=True, reason="ok"))
    verdict = await judges.judge_relevance("q", [_c()], llm=llm)
    assert verdict.sufficient is True


async def test_judge_relevance_parses_insufficient():
    llm = FakeLLM(judges.RelevanceVerdict(sufficient=False, reason="off-topic"))
    verdict = await judges.judge_relevance("q", [_c()], llm=llm)
    assert verdict.sufficient is False


async def test_judge_groundedness_parses_grounded():
    llm = FakeLLM(judges.GroundednessVerdict(grounded=True, reason="ok"))
    verdict = await judges.judge_groundedness("La consulta dura 60 min.", [_c()], llm=llm)
    assert verdict.grounded is True


async def test_judge_groundedness_parses_ungrounded():
    llm = FakeLLM(judges.GroundednessVerdict(grounded=False, reason="inventado"))
    verdict = await judges.judge_groundedness("La consulta dura 90 min.", [_c()], llm=llm)
    assert verdict.grounded is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_judges.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.rag.judges'`.

- [ ] **Step 3a: Agregar el helper compartido `chunks_text` a `backend/app/rag/synthesize.py`**

Junto a `build_sources` (usa `Chunk`, ya importado en el módulo):

```python
def chunks_text(chunks: list[Chunk]) -> str:
    """Formatea chunks como lista para prompts de jueces/reformulador (sin marcas de cita)."""
    return "\n".join(f'- ({c["title"]}) {c["text"]}' for c in chunks)
```

- [ ] **Step 3b: Create `backend/app/rag/judges.py`**

```python
from typing import Any

from pydantic import BaseModel

from app.llm import make_llm
from app.models import Chunk
from app.rag.synthesize import chunks_text

RELEVANCE_PROMPT = (
    "Sos un evaluador de relevancia de un CRM para prácticas profesionales. Dada una "
    "pregunta y un conjunto de fragmentos recuperados, decidí si los fragmentos contienen "
    "información SUFICIENTE para responder la pregunta. Respondé sufficient=true solo si la "
    "respuesta puede fundamentarse en los fragmentos; si están off-topic o falta el dato, "
    "sufficient=false. Incluí una razón breve en español."
)

GROUNDEDNESS_PROMPT = (
    "Sos un verificador de fundamentación de un CRM para prácticas profesionales. Dada una "
    "respuesta y los fragmentos fuente, decidí si CADA afirmación de la respuesta está "
    "respaldada por los fragmentos. grounded=true solo si todo lo afirmado se puede verificar "
    "en los fragmentos; si hay datos inventados o no presentes, grounded=false. Incluí una "
    "razón breve en español."
)


class RelevanceVerdict(BaseModel):
    sufficient: bool
    reason: str


class GroundednessVerdict(BaseModel):
    grounded: bool
    reason: str


def _judge_llm() -> Any:
    return make_llm("gemma4:e4b", temperature=0.0)


async def judge_relevance(query: str, chunks: list[Chunk], llm: Any = None) -> RelevanceVerdict:
    llm = llm or _judge_llm()
    structured = llm.with_structured_output(RelevanceVerdict)
    human = f"Pregunta: {query}\n\nFragmentos:\n{chunks_text(chunks)}"
    verdict: RelevanceVerdict = await structured.ainvoke(
        [("system", RELEVANCE_PROMPT), ("human", human)]
    )
    return verdict


async def judge_groundedness(
    answer: str, chunks: list[Chunk], llm: Any = None
) -> GroundednessVerdict:
    llm = llm or _judge_llm()
    structured = llm.with_structured_output(GroundednessVerdict)
    human = f"Respuesta:\n{answer}\n\nFragmentos:\n{chunks_text(chunks)}"
    verdict: GroundednessVerdict = await structured.ainvoke(
        [("system", GROUNDEDNESS_PROMPT), ("human", human)]
    )
    return verdict
```

- [ ] **Step 4: Run test to verify it passes**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_judges.py -q`
Expected: PASS (4 tests).

- [ ] **Step 5: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add backend/app/rag/judges.py backend/tests/test_judges.py backend/app/rag/synthesize.py
git commit -m "feat(rag): jueces de relevancia/groundedness + helper chunks_text compartido"
```

---

### Task 4: `rag/reformulate.py` — reescritura de query

**Files:**
- Create: `backend/app/rag/reformulate.py`
- Create: `backend/tests/test_reformulate.py`

**Interfaces:**
- Consumes: `make_llm`, `Chunk`, `chunks_text` (`app.rag.synthesize`, agregado en Task 3).
- Produces:
  - `class Reformulation(BaseModel): query: str`
  - `async def reformulate(original_query: str, weak_chunks: list[Chunk], llm: Any = None) -> str`

- [ ] **Step 1: Write the failing test** — `backend/tests/test_reformulate.py`

```python
from app.rag import reformulate


class FakeStructured:
    def __init__(self, value):
        self._value = value

    async def ainvoke(self, messages):
        return self._value


class FakeLLM:
    def __init__(self, value):
        self._value = value

    def with_structured_output(self, schema):
        return FakeStructured(self._value)


async def test_reformulate_returns_query_string():
    llm = FakeLLM(reformulate.Reformulation(query="duracion primera consulta turno"))
    out = await reformulate.reformulate("¿cuánto dura?", [], llm=llm)
    assert out == "duracion primera consulta turno"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_reformulate.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.rag.reformulate'`.

- [ ] **Step 3: Create `backend/app/rag/reformulate.py`**

```python
from typing import Any

from pydantic import BaseModel

from app.llm import make_llm
from app.models import Chunk
from app.rag.synthesize import chunks_text

REFORMULATE_PROMPT = (
    "Sos un reformulador de consultas de búsqueda para un CRM de prácticas profesionales. La "
    "búsqueda anterior no recuperó contexto suficiente. Reescribí la pregunta del usuario como "
    "una consulta de búsqueda MEJOR en español: más específica, con sinónimos y términos del "
    "dominio (turnos, protocolo, ficha, consulta, cancelación, etc.). Devolvé solo la nueva "
    "consulta, sin explicaciones."
)


class Reformulation(BaseModel):
    query: str


def _reformulate_llm() -> Any:
    return make_llm("gemma4:e4b", temperature=0.0)


async def reformulate(original_query: str, weak_chunks: list[Chunk], llm: Any = None) -> str:
    llm = llm or _reformulate_llm()
    structured = llm.with_structured_output(Reformulation)
    human = (
        f"Pregunta original: {original_query}\n\n"
        f"Fragmentos recuperados (insuficientes):\n{chunks_text(weak_chunks)}"
    )
    result: Reformulation = await structured.ainvoke(
        [("system", REFORMULATE_PROMPT), ("human", human)]
    )
    return result.query
```

- [ ] **Step 4: Run test to verify it passes**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_reformulate.py -q`
Expected: PASS.

- [ ] **Step 5: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add backend/app/rag/reformulate.py backend/tests/test_reformulate.py
git commit -m "feat(rag): reformulador de query (e4b, salida estructurada)"
```

---

### Task 5: `rag/synthesize.py` — variante buffered `synthesize()`

**Files:**
- Modify: `backend/app/rag/synthesize.py` (agregar `synthesize`)
- Modify: `backend/tests/test_synthesize.py` (agregar 2 tests)

**Interfaces:**
- Consumes: `synthesize_stream` existente, `ABSTAIN_MESSAGE`.
- Produces: `async def synthesize(query: str, chunks: list[Chunk], llm: Any = None) -> str` — colecta `synthesize_stream` a un string (buffer-then-stream).

- [ ] **Step 1: Write the failing tests** — agregar al final de `backend/tests/test_synthesize.py`

```python
async def test_synthesize_buffered_collects_stream():
    out = await synthesize.synthesize("¿cuánto dura?", [_chunk()], llm=FakeLLM())
    assert "[1]" in out


async def test_synthesize_buffered_abstains_without_context():
    out = await synthesize.synthesize("hola", [])
    assert out == synthesize.ABSTAIN_MESSAGE
```

- [ ] **Step 2: Run test to verify it fails**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_synthesize.py -q`
Expected: FAIL — `AttributeError: module 'app.rag.synthesize' has no attribute 'synthesize'`.

- [ ] **Step 3: Implement** — agregar a `backend/app/rag/synthesize.py` (después de `synthesize_stream`)

```python
async def synthesize(query: str, chunks: list[Chunk], llm: Any = None) -> str:
    """Variante buffered: colecta synthesize_stream a un string. Necesaria para
    buffer-then-stream — la respuesta se verifica (groundedness) antes de emitirse."""
    return "".join([piece async for piece in synthesize_stream(query, chunks, llm=llm)])
```

- [ ] **Step 4: Run test to verify it passes**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_synthesize.py -q`
Expected: PASS.

- [ ] **Step 5: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add backend/app/rag/synthesize.py backend/tests/test_synthesize.py
git commit -m "feat(rag): synthesize() buffered para buffer-then-stream"
```

---

### Task 6: `graph/rag_subgraph.py` — el subgrafo CRAG (corazón)

**Files:**
- Create: `backend/app/graph/rag_subgraph.py`
- Create: `backend/tests/test_rag_subgraph.py`
- Modify: `backend/app/config.py` (agregar `rag_fetch_k`, `rag_max_attempts`)

**Interfaces:**
- Consumes: `retrieve`, `rerank`, `judge_relevance`, `judge_groundedness`, `reformulate`, `synthesize`, `build_sources`, `ABSTAIN_MESSAGE`; `Chunk`; config `rag_fetch_k`, `rag_max_attempts`.
- Produces:
  - `class RagState(TypedDict)` con: `original_query, query, practice_id, attempts, reranked, sufficient, answer, grounded, abstained, sources`.
  - `def initial_rag_state(query: str, practice_id: str) -> RagState`
  - `crag_app` (grafo compilado): `await crag_app.ainvoke(initial_rag_state(q, pid))` devuelve la `RagState` final con `answer`, `sources`, `grounded`, `abstained`, `reranked`.
- **Nota de monkeypatch (tests):** los nodos llaman a las funciones de `rag/` como globals del módulo; los tests las parchean **en el namespace de `rag_subgraph`** (`rag_subgraph.rerank`, `rag_subgraph.judge_relevance`, …) — patrón de `test_nodes.py`.

- [ ] **Step 1: Add config fields** — `backend/app/config.py`, junto a los de Task 2:

```python
    rag_fetch_k: int = 20
    rag_max_attempts: int = 2
```

- [ ] **Step 2: Write the failing tests** — `backend/tests/test_rag_subgraph.py`

```python
from app.graph import rag_subgraph
from app.models import Chunk
from app.rag import judges
from app.rag.synthesize import ABSTAIN_MESSAGE


def _c(doc_id: str = "1") -> Chunk:
    return Chunk(
        text="La consulta dura 60 min.", page=None, chunk_index=0,
        document_id=doc_id, title="Protocolo", doc_type="protocolo",
    )


def _patch(monkeypatch, **fns):
    for name, fn in fns.items():
        monkeypatch.setattr(rag_subgraph, name, fn)


async def _ok_retrieve(query, practice_id=None, top_k=None):
    return [_c()]


async def _ok_rerank(query, chunks):
    return chunks


async def test_sufficient_first_try_returns_grounded_answer(monkeypatch):
    async def synth(q, chunks):
        return "La consulta dura 60 min [1]."

    async def jr(q, chunks, llm=None):
        return judges.RelevanceVerdict(sufficient=True, reason="ok")

    async def jg(a, chunks, llm=None):
        return judges.GroundednessVerdict(grounded=True, reason="ok")

    _patch(monkeypatch, retrieve=_ok_retrieve, rerank=_ok_rerank,
           judge_relevance=jr, synthesize=synth, judge_groundedness=jg)
    out = await rag_subgraph.crag_app.ainvoke(
        rag_subgraph.initial_rag_state("¿cuánto dura?", "p")
    )
    assert out["abstained"] is False
    assert "[1]" in out["answer"]
    assert out["sources"] == [
        {"n": 1, "title": "Protocolo", "page": None, "document_id": "1"}
    ]


async def test_insufficient_then_reformulate_then_sufficient(monkeypatch):
    calls = {"r": 0, "reform": 0}

    async def jr(q, chunks, llm=None):
        calls["r"] += 1
        return judges.RelevanceVerdict(sufficient=calls["r"] >= 2, reason="x")

    async def synth(q, chunks):
        return "ok [1]."

    async def jg(a, chunks, llm=None):
        return judges.GroundednessVerdict(grounded=True, reason="ok")

    async def reform(orig, chunks):
        calls["reform"] += 1
        return "mejor query"

    _patch(monkeypatch, retrieve=_ok_retrieve, rerank=_ok_rerank, judge_relevance=jr,
           synthesize=synth, judge_groundedness=jg, reformulate=reform)
    out = await rag_subgraph.crag_app.ainvoke(rag_subgraph.initial_rag_state("q", "p"))
    assert calls["reform"] == 1
    assert out["abstained"] is False
    assert out["sources"]


async def test_insufficient_twice_abstains_without_sources(monkeypatch):
    retr = {"n": 0}

    async def retrieve(query, practice_id=None, top_k=None):
        retr["n"] += 1
        return [_c()]

    async def jr(q, chunks, llm=None):
        return judges.RelevanceVerdict(sufficient=False, reason="no")

    async def reform(orig, chunks):
        return "otra"

    _patch(monkeypatch, retrieve=retrieve, rerank=_ok_rerank,
           judge_relevance=jr, reformulate=reform)
    out = await rag_subgraph.crag_app.ainvoke(rag_subgraph.initial_rag_state("q", "p"))
    assert retr["n"] == 2
    assert out["abstained"] is True
    assert out["answer"] == ABSTAIN_MESSAGE
    assert out["sources"] == []


async def test_ungrounded_answer_abstains_without_sources(monkeypatch):
    async def synth(q, chunks):
        return "La consulta dura 90 min [1]."

    async def jr(q, chunks, llm=None):
        return judges.RelevanceVerdict(sufficient=True, reason="ok")

    async def jg(a, chunks, llm=None):
        return judges.GroundednessVerdict(grounded=False, reason="inventado")

    _patch(monkeypatch, retrieve=_ok_retrieve, rerank=_ok_rerank,
           judge_relevance=jr, synthesize=synth, judge_groundedness=jg)
    out = await rag_subgraph.crag_app.ainvoke(rag_subgraph.initial_rag_state("q", "p"))
    assert out["abstained"] is True
    assert out["answer"] == ABSTAIN_MESSAGE
    assert out["sources"] == []


async def test_synth_self_abstain_skips_groundedness(monkeypatch):
    ground = {"called": False}

    async def synth(q, chunks):
        return ABSTAIN_MESSAGE

    async def jr(q, chunks, llm=None):
        return judges.RelevanceVerdict(sufficient=True, reason="ok")

    async def jg(a, chunks, llm=None):
        ground["called"] = True
        return judges.GroundednessVerdict(grounded=True, reason="ok")

    _patch(monkeypatch, retrieve=_ok_retrieve, rerank=_ok_rerank,
           judge_relevance=jr, synthesize=synth, judge_groundedness=jg)
    out = await rag_subgraph.crag_app.ainvoke(rag_subgraph.initial_rag_state("q", "p"))
    assert ground["called"] is False
    assert out["abstained"] is True
    assert out["sources"] == []
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_rag_subgraph.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.graph.rag_subgraph'`.

- [ ] **Step 4: Create `backend/app/graph/rag_subgraph.py`**

```python
import logging
from typing import Any, TypedDict

from langgraph.graph import END, START, StateGraph

from app.config import get_settings
from app.models import Chunk
from app.rag.judges import judge_groundedness, judge_relevance
from app.rag.reformulate import reformulate
from app.rag.rerank import rerank
from app.rag.retrieve import retrieve
from app.rag.synthesize import ABSTAIN_MESSAGE, build_sources, synthesize

logger = logging.getLogger(__name__)


class RagState(TypedDict):
    original_query: str  # la pregunta del usuario (no muta)
    query: str  # query de búsqueda actual (puede reformularse)
    practice_id: str
    attempts: int  # intentos de retrieve consumidos
    reranked: list[Chunk]  # top_k tras rerank+floor del intento actual
    sufficient: bool  # veredicto del juez de relevancia
    answer: str  # borrador sintetizado (buffered)
    grounded: bool  # veredicto del juez de groundedness
    abstained: bool  # True si terminó en abstención
    sources: list[dict]  # build_sources(reranked) — solo se llena en éxito


def initial_rag_state(query: str, practice_id: str) -> RagState:
    return {
        "original_query": query,
        "query": query,
        "practice_id": practice_id,
        "attempts": 0,
        "reranked": [],
        "sufficient": False,
        "answer": "",
        "grounded": False,
        "abstained": False,
        "sources": [],
    }


async def retrieve_node(state: RagState) -> dict[str, Any]:
    s = get_settings()
    candidates = await retrieve(
        state["query"], practice_id=state["practice_id"], top_k=s.rag_fetch_k
    )
    reranked = await rerank(state["query"], candidates)
    return {"reranked": reranked, "attempts": state["attempts"] + 1}


async def grade_node(state: RagState) -> dict[str, Any]:
    if not state["reranked"]:
        return {"sufficient": False}
    try:
        verdict = await judge_relevance(state["original_query"], state["reranked"])
        return {"sufficient": verdict.sufficient}
    except Exception:
        logger.warning("juez de relevancia falló; trato como insuficiente", exc_info=True)
        return {"sufficient": False}


def grade_router(state: RagState) -> str:
    if state["sufficient"]:
        return "synthesize"
    if state["attempts"] < get_settings().rag_max_attempts:
        return "reformulate"
    return "abstain"


async def reformulate_node(state: RagState) -> dict[str, Any]:
    new_query = await reformulate(state["original_query"], state["reranked"])
    return {"query": new_query}


async def synthesize_node(state: RagState) -> dict[str, Any]:
    answer = await synthesize(state["original_query"], state["reranked"])
    return {"answer": answer}


def synth_router(state: RagState) -> str:
    if state["answer"].strip() == ABSTAIN_MESSAGE:
        return "abstain"
    return "groundedness"


async def groundedness_node(state: RagState) -> dict[str, Any]:
    try:
        verdict = await judge_groundedness(state["answer"], state["reranked"])
        grounded = verdict.grounded
    except Exception:
        logger.warning("juez de groundedness falló; trato como no fundamentado", exc_info=True)
        grounded = False
    if grounded:
        return {
            "grounded": True,
            "abstained": False,
            "sources": build_sources(state["reranked"]),
        }
    return {"grounded": False}


def ground_router(state: RagState) -> str:
    return "finalize" if state["grounded"] else "abstain"


async def abstain_node(state: RagState) -> dict[str, Any]:
    return {"abstained": True, "answer": ABSTAIN_MESSAGE, "sources": []}


def build_crag() -> Any:
    g = StateGraph(RagState)
    g.add_node("retrieve", retrieve_node)
    g.add_node("grade", grade_node)
    g.add_node("reformulate", reformulate_node)
    g.add_node("synthesize", synthesize_node)
    g.add_node("groundedness", groundedness_node)
    g.add_node("abstain", abstain_node)

    g.add_edge(START, "retrieve")
    g.add_edge("retrieve", "grade")
    g.add_conditional_edges(
        "grade",
        grade_router,
        {"synthesize": "synthesize", "reformulate": "reformulate", "abstain": "abstain"},
    )
    g.add_edge("reformulate", "retrieve")
    g.add_conditional_edges(
        "synthesize",
        synth_router,
        {"groundedness": "groundedness", "abstain": "abstain"},
    )
    g.add_conditional_edges(
        "groundedness",
        ground_router,
        {"finalize": END, "abstain": "abstain"},
    )
    g.add_edge("abstain", END)
    return g.compile()


crag_app = build_crag()
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_rag_subgraph.py -q`
Expected: PASS (5 tests).

- [ ] **Step 6: Lint + types**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app`
Expected: sin errores.

- [ ] **Step 7: Commit**

```bash
git add backend/app/graph/rag_subgraph.py backend/tests/test_rag_subgraph.py backend/app/config.py
git commit -m "feat(graph): subgrafo CRAG (retrieve+rerank, jueces, reformular, abstener)"
```

---

### Task 7: `graph/nodes.py` — `rag_node` invoca el subgrafo y emite el SSE

**Files:**
- Modify: `backend/app/graph/nodes.py` (reescribir `rag_node` + imports)
- Modify: `backend/tests/test_nodes.py` (reemplazar los 2 tests de `rag_node`)

**Interfaces:**
- Consumes: `crag_app`, `initial_rag_state` (`app.graph.rag_subgraph`).
- Produces: `rag_node` emite tokens del `answer` aprobado y `sources` **solo en el camino grounded**; toda abstención emite `write_sources([])`.

- [ ] **Step 1: Reescribir los tests de `rag_node`** en `backend/tests/test_nodes.py`

Eliminá los dos tests existentes `test_rag_node_streams_tokens_and_sources` y `test_rag_node_abstains_without_chunks` (monkeypatchean `nodes.retrieve`/`nodes.synthesize_stream`, que dejan de existir en `rag_node`). Agregá el import y los reemplazos:

```python
from app.rag.synthesize import ABSTAIN_MESSAGE


class FakeCragApp:
    def __init__(self, result: dict):
        self._result = result

    async def ainvoke(self, state):
        return self._result


async def test_rag_node_emits_answer_and_sources(monkeypatch):
    result = {
        "abstained": False,
        "answer": "Según el protocolo [1].",
        "sources": [{"n": 1, "title": "Protocolo", "page": 2, "document_id": "doc-1"}],
        "reranked": [_chunk()],
    }
    monkeypatch.setattr(nodes, "crag_app", FakeCragApp(result))
    tokens, sources = await _run(nodes.rag_node, new_state("¿cuánto dura?", "p", "t"))
    assert "[1]" in tokens
    assert sources == [{"n": 1, "title": "Protocolo", "page": 2, "document_id": "doc-1"}]


async def test_rag_node_abstains_emits_no_sources(monkeypatch):
    result = {"abstained": True, "answer": ABSTAIN_MESSAGE, "sources": [], "reranked": []}
    monkeypatch.setattr(nodes, "crag_app", FakeCragApp(result))
    tokens, sources = await _run(nodes.rag_node, new_state("algo raro", "p", "t"))
    assert tokens == ABSTAIN_MESSAGE
    assert sources == []
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_nodes.py -q`
Expected: FAIL — `AttributeError: module 'app.graph.nodes' has no attribute 'crag_app'` (aún no reescribimos `rag_node`).

- [ ] **Step 3: Reescribir `rag_node` e imports en `backend/app/graph/nodes.py`**

Reemplazá los imports de la cabecera:

```python
from app.config import get_settings
from app.graph.rag_subgraph import crag_app, initial_rag_state
from app.graph.state import AgentState, last_user_text
```

(Quitá `from app.rag.retrieve import retrieve` y `from app.rag.synthesize import ABSTAIN_MESSAGE, build_sources, synthesize_stream` — `rag_node` ya no los usa directamente y ruff marcaría imports sin uso.)

Reemplazá la función `rag_node` por:

```python
def _stream_chunks(text: str, size: int = 24) -> list[str]:
    if not text:
        return [""]
    return [text[i : i + size] for i in range(0, len(text), size)]


async def rag_node(state: AgentState) -> dict:
    result = await crag_app.ainvoke(
        initial_rag_state(last_user_text(state), state["practice_id"])
    )
    answer = result["answer"]
    if result["abstained"]:
        write_token(answer)
        write_sources([])
        return {
            "retrieved": result["reranked"],
            "sources": [],
            "messages": [AIMessage(content=answer)],
        }
    for piece in _stream_chunks(answer):
        write_token(piece)
    write_sources(result["sources"])
    return {
        "retrieved": result["reranked"],
        "sources": result["sources"],
        "messages": [AIMessage(content=answer)],
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `backend\.venv\Scripts\python -m pytest backend/tests/test_nodes.py -q`
Expected: PASS (todos: scope/sql_stub/action_stub/chitchat sin cambios + los 2 nuevos de rag_node).

- [ ] **Step 5: Lint + types + suite no-llm completa**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app; backend\.venv\Scripts\python -m pytest backend/tests -m "not llm" -q`
Expected: sin errores; toda la suite no-llm verde.

- [ ] **Step 6: Commit**

```bash
git add backend/app/graph/nodes.py backend/tests/test_nodes.py
git commit -m "feat(graph): rag_node invoca el subgrafo CRAG y emite el SSE (buffer-then-stream)"
```

---

### Task 8: End-to-end LLM + golden set + gate final

**Files:**
- Modify: `backend/tests/test_e2e_llm.py` (reforzar el test de abstención: sin fuentes)
- Create: `backend/app/eval/golden_set.jsonl`

**Interfaces:**
- Consumes: el flujo completo `/chat → grafo → rag_node → CRAG` (real, con Ollama+Qdrant).

- [ ] **Step 1: Reforzar el test de abstención end-to-end** — `backend/tests/test_e2e_llm.py`

En `test_real_llm_abstains_when_answer_not_in_context`, cambiá la última parte para capturar y asertar **fuentes vacías** (es el fix del bug "abstención que muestra fuentes que no usó"):

```python
    answer, sources = _parse_sse(body)
    assert "No encuentro esa información" in answer
    assert sources == []
```

- [ ] **Step 2: Crear el golden set** — `backend/app/eval/golden_set.jsonl`

(Semilla para el gate de Ragas de Fase 2; sin runner aún. Una línea por caso.)

```jsonl
{"question": "¿cuánto dura la primera consulta?", "seed_doc": "protocolo", "expected_behavior": "cited_answer", "must_include": ["60"]}
{"question": "¿cuál es la dirección del consultorio?", "seed_doc": "protocolo", "expected_behavior": "abstain_no_sources", "must_include": ["No encuentro esa información"]}
```

- [ ] **Step 3: Gate no-llm completo (lo que el ejecutor puede correr sin Ollama)**

Run: `backend\.venv\Scripts\python -m ruff check backend/app backend/tests; backend\.venv\Scripts\python -m ruff format --check backend/app backend/tests; backend\.venv\Scripts\python -m mypy backend/app; backend\.venv\Scripts\python -m pytest backend/tests -m "not llm" -q`
Expected: todo verde.

- [ ] **Step 4: Gate LLM end-to-end (requiere entorno; ver nota)**

Requiere: `docker compose up -d` (Postgres+Qdrant), Ollama corriendo con `gemma4:12b` y `gemma4:e4b`, y **la primera corrida descarga `bge-reranker-v2-m3` (~600MB)** vía sentence-transformers (red permitida solo para bajar modelos, CLAUDE.md §0).

Run: `backend\.venv\Scripts\python -m pytest backend/tests -m llm -q`
Expected: citación con `60` + fuentes no vacías; abstención off-topic sin fuentes.

> Si el ejecutor no tiene Ollama/infra, **dejá este step para el usuario** y reportalo explícitamente (no marques el plan como completo en falso).

- [ ] **Step 5: Smoke manual (usuario)** — CLAUDE.md §2 / `frontend/SMOKE.md`

Backend `backend\.venv\Scripts\python backend\dev.py` + `npm --prefix frontend run dev`. Probar: `"hola"`→chitchat; consulta documental→respuesta citada (con el doc semilla); pregunta off-topic→abstención **sin fuentes en el canvas**. Verificar que las escrituras (sql/action) siguen mostrando el stub (no escriben).

- [ ] **Step 6: Commit**

```bash
git add backend/tests/test_e2e_llm.py backend/app/eval/golden_set.jsonl
git commit -m "test(rag): abstencion e2e sin fuentes y golden set semilla para CRAG"
```

---

## Notas de integración

- **`build.py` no se toca**: el nodo `"rag"` ya está cableado (`g.add_node("rag", rag_node)`); solo cambian las internas de `rag_node`.
- **Contrato SSE del front sin cambios** (`token`/`sources`/`done`). El indicador "verificando fuentes…" queda como nice-to-have diferido (canvas más rico).
- **Diferidos explícitos** (no construir en este slice): retrieval híbrido denso+sparse, DSPy (prompts compilados), semantic/embed cache, trazas Phoenix, gate formal de Ragas.

## Self-Review (cobertura del spec)

- retrieve más candidatos → rerank → juez relevancia → reformular/reintentar → síntesis buffered → juez groundedness → emitir/abstener: **Tasks 2–7**. ✔
- Subgrafo puro + nodo emite (buffer-then-stream): **Task 6 (puro) + Task 7 (emisión)**. ✔
- Veredicto de relevancia binario; cap 1 reformulación (`rag_max_attempts=2`): **Task 6** (`grade_router`). ✔
- Fail-closed (jueces/reranker): **Task 2** (fallback denso) + **Task 6** (try/except → insuficiente / no-grounded). ✔
- Bug "abstención con fuentes": fuentes solo en grounded; toda abstención `sources=[]`: **Task 6 + 7 + 8** (e2e). ✔
- Config nueva, sin deps nuevas, DRY de ChatOllama: **Tasks 1, 2, 6**. ✔
- Testing no-llm (inyección/monkeypatch) + `-m llm` + golden set: **Tasks 2–8**. ✔
- Multi-tenant (`practice_id` en retrieve): **Task 6** (`retrieve_node`). ✔
